# V0 Design Analysis for Streamlit Implementation

## 🎯 전체 디자인 철학

### 핵심 원칙
1. **정보 밀도 우선** - 트레이딩 툴처럼 한 화면에 최대한 많은 정보
2. **Master-Detail 구조** - 리스트 선택 → 상세 정보 흐름
3. **시각적 계층** - 색상보다 레이아웃과 타이포그래피로 구분
4. **액션 중심** - 각 화면마다 명확한 CTA (Call-to-Action)

### 공통 레이아웃 패턴
- **사이드바 (고정, 264px)**: Quick Stats + Navigation + AI Engine Status
- **메인 영역**: Page Title + Description → Content → Actions
- **다크 테마**: 블랙 배경 + 고대비 텍스트 (트레이딩 툴 스타일)

---

## 📊 페이지별 상세 분석

### 1. Dashboard (홈)

#### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ Dashboard                                            │
│ System overview and next actions                     │
├─────────────────────────────────────────────────────┤
│ [KPI 1] [KPI 2] [KPI 3] [KPI 4]                     │  ← 4-column metrics
├─────────────────────────────────────────────────────┤
│ Workflow Progress                                    │
│ ○━━━○━━━○ ··· ○ ··· ○                              │  ← 5-step pipeline
├─────────────────────────────────────────────────────┤
│ Action Needed       │ Status Distribution            │  ← 2-column layout
│ • Item 1 [View]     │ ▬▬▬▬▬ Approved 40%            │
│ • Item 2 [View]     │ ▬▬▬▬  Monitoring 35%          │
│ • Item 3 [View]     │ ▬▬    Pending 25%             │
│                     │                                │
│                     │ AI Score Range                 │
│                     │ High (80-100): 8 stocks        │
│                     │ Medium (60-79): 7 stocks       │
└─────────────────────────────────────────────────────┘
```

#### 구성 요소
1. **KPI Cards (4개)** - Stock Pool / AI Reports / Approval Queue / Active Trades
2. **Workflow Progress** - 5단계 파이프라인 시각화
3. **Action Needed** - 우선순위 액션 리스트 (High Priority 배지)
4. **Status Distribution** - 2개 차트 (상태 분포 + AI 점수 범위)

#### Streamlit 구현
- `st.columns(4)` + `st.metric()` for KPI cards
- Custom progress bar using `st.columns(5)` for workflow
- `st.columns([2, 1])` for Action Needed + Status Distribution
- Action items: `st.info()` with button in columns

---

### 2. AI Reports (Master-Detail)

#### 레이아웃 구조
```
┌───────────┬─────────────────────────────────────────┐
│ AI Reports│ Top 20 analyzed stocks                  │
├───────────┼─────────────────────────────────────────┤
│ [1] 005930│ Samsung Electronics              [BUY]  │
│    ▲+3.2% │                                          │
│    Score92│ ┌─────────────────────────────────────┐ │
│ Conf: 88% │ │ Summary │ Momentum │ Liquidity │ Risk│ │
│           │ ├─────────────────────────────────────┤ │
├───────────┤ │ Analysis Summary                     │ │
│ [2] 035420│ │ Strong momentum with institutional..  │ │
│    ▲+2.8% │ │                                      │ │
│    Score89│ │ Technical Rating: Strong  ▬▬▬▬▬ 85%  │ │
│           │ │ Fundamental: Positive     ▬▬▬▬  78%  │ │
├───────────┤ │                                      │ │
│ [3] ...   │ │ Key Factors:                         │ │
│           │ │ ✓ Strong institutional buying        │ │
│           │ │ ✓ Breakout above 200-day MA          │ │
│           │ └─────────────────────────────────────┘ │
│           │                                          │
│           │ [Approve] [Keep Monitoring] [Reject]    │
└───────────┴─────────────────────────────────────────┘
  Left 30%              Right 70%
```

#### 구성 요소
**Left Panel (Master List)**
- Top 20 리스트, 스크롤 가능
- 각 아이템: Rank + Ticker + Name + Badge + Score + Confidence + 한 줄 요약
- 선택된 아이템 하이라이트

**Right Panel (Detail)**
- Header: Company Name + Ticker + AI Score (92)
- 4 Tabs: Summary / Momentum / Liquidity / Risk
- Content: 텍스트 + 2-column metrics + Progress bars + Bullet list
- Actions: 3 buttons (Approve / Keep Monitoring / Reject)

#### Streamlit 구현
- `st.columns([1, 2])` for Master-Detail split
- Left: Loop through reports with `st.container()` + `st.button("Select")`
- Right: `st.tabs()` for 4-tab structure
- `st.session_state` for tracking selected report

---

### 3. Stock Pool (Data Table)

#### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ Stock Pool                                           │
│ Monitoring 500 of 500 stocks                         │
├─────────────────────────────────────────────────────┤
│ [Search...] [All Sectors ▼] [All Status ▼]         │
├─────────────────────────────────────────────────────┤
│ Ticker │ Company    │ Sector │ Price │ Chg% │ Vol │ │
├────────┼────────────┼────────┼───────┼──────┼─────┤ │
│ STK001 │ Company 1  │ Health │ 33371 │-1.7% │ 5.9M│ │
│ STK002 │ Company 2  │ Energy │ 71582 │+0.8% │ 7.8M│ │
│ STK003 │ Company 3  │ Tech   │ 97843 │+0.3% │ 9.4M│ │
│ ...    │ ...        │ ...    │ ...   │ ...  │ ... │ │
└─────────────────────────────────────────────────────┘
```

#### 구성 요소
1. **Search Bar** - 티커/회사명 검색
2. **2 Filters** - Sector dropdown + Status dropdown
3. **Data Table** - 8 columns (Ticker / Company / Sector / Price / Change% / Volume / AI Score / Status)
4. **Status Badges** - analyzing / rejected / watching / qualified (색상 구분)

#### Streamlit 구현
- `st.columns([3, 1, 1])` for search + 2 filters
- `st.dataframe()` with custom styling
- DataFrame filtering based on search + dropdown values
- Status badges: Use emoji or colored text in DataFrame

---

### 4. Trading (Order Form + Info)

#### 레이아웃 구조
```
┌─────────────────────────────┬───────────────────────┐
│ Trading                      │ Account Balance       │
│ Execute trades and manage... │                       │
├─────────────────────────────┤ Available Cash        │
│ New Order                    │ ₩125,430,000          │
│                              │                       │
│ Stock: [Search...]           │ Buying Power          │
│                              │ ₩501,720,000          │
│ [Buy] / Sell                 │                       │
│                              │ Margin Used: 35%      │
│ Order Type: [Market Order▼] ├───────────────────────┤
│ Quantity: [0]                │ Market Status         │
│                              │                       │
│ Estimated Total: ₩0          │ KOSPI: 2,647.35 +0.8% │
│ Commission: ₩0               │ KOSDAQ: 782.14 -0.3%  │
│ Total Amount: ₩0             │                       │
│                              ├───────────────────────┤
│ [Buy Button]     [Reset]     │ Trading Limits        │
├─────────────────────────────┤                       │
│ Recent Orders                │ Max Position: ₩50M    │
│                              │ Max Daily Loss: ₩10M  │
│ (No recent orders)           │ Max Leverage: 4x      │
└─────────────────────────────┴───────────────────────┘
     Left 65%                       Right 35%
```

#### 구성 요소
**Left Panel**
- New Order Form (Stock search / Buy-Sell toggle / Order Type / Quantity / Total)
- Recent Orders list (empty state)

**Right Panel**
- Account Balance (Available Cash / Buying Power / Margin Used)
- Market Status (KOSPI / KOSDAQ indices)
- Trading Limits (Max Position / Max Daily Loss / Max Leverage)

#### Streamlit 구현
- `st.columns([2, 1])` for form + info split
- Left: `st.text_input()`, `st.selectbox()`, `st.number_input()`, `st.button()`
- Right: `st.metric()` for account balance and market status
- Toggle: Use `st.radio()` horizontal or custom button group

---

### 5. Approval Queue (Master-Detail)

#### 레이아웃 구조
```
┌───────────────────────┬─────────────────────────────┐
│ Approval Queue         │ Quick Analysis              │
│ Review and approve...  │                             │
├───────────────────────┼─────────────────────────────┤
│ Pending Approvals (3) │ 005930                      │
│                        │ Samsung Electronics         │
│ [005930] Score: 92     │                             │
│ Samsung Electronics    │ Current Price: ₩68,500      │
│ ₩68,500  ▲+3.2%       │ AI Score: 92                │
│            2025-01-15  │ Price Change: +3.2%         │
│                        │                             │
├───────────────────────┤ Technical Indicators         │
│ [035420] Score: 89     │ RSI: 68.5 (Bullish)         │
│ NAVER                  │ MACD: Bullish               │
│ ₩185,000  ▲+2.8%      │ Volume: High                │
│            2025-01-15  │                             │
│                        ├─────────────────────────────┤
├───────────────────────┤ Risk Assessment             │
│ [035720] Score: 86     │ ⚠ Moderate volatility       │
│ Kakao                  │ Suggested position: 2-3%    │
│ ₩45,200  ▲+4.1%       │                             │
│            2025-01-14  │ [Start Trading]             │
│                        │ [Re-evaluate]               │
│                        │ [Remove from Queue]         │
└───────────────────────┴─────────────────────────────┘
     Left 55%                  Right 45%
```

#### 구성 요소
**Left Panel**
- Pending Approvals list (3 items)
- Each item: Ticker + Score + Name + Price + Change% + Date

**Right Panel**
- Quick Analysis for selected stock
- Price info + AI Score + Technical Indicators
- Risk Assessment
- 3 action buttons

#### Streamlit 구현
- `st.columns([1.2, 1])` for Master-Detail
- Similar to AI Reports but simpler
- Left: List with selection buttons
- Right: Metrics + text + 3 action buttons

---

### 6. Active Trades (KPI + Table)

#### 레이아웃 구조
```
┌─────────────────────────────────────────────────────┐
│ Active Trades                                        │
│ Monitoring 8 open positions                          │
├─────────────────────────────────────────────────────┤
│ [Total P&L]  [Avg P&L]  [Total Value]  [Positions]  │  ← 4-column KPIs
│ +₩816,000    +1.03%     ₩87,300,000    8            │
├─────────────────────────────────────────────────────┤
│ Active Positions                                     │
├──────┬──────────┬───────┬────────┬─────┬─────┬──────┤
│Ticker│ Company  │ Entry │ Current│ Qty │ P&L │ Date │
├──────┼──────────┼───────┼────────┼─────┼─────┼──────┤
│STK023│ Samsung  │ 68,500│ 71,200 │ 150 │+405K│12-28 │
│STK089│ SK Hynix │142,000│138,500 │  80 │-280K│12-27 │
│STK156│ Hyundai  │187,000│194,300 │  50 │+365K│12-29 │
│ ...  │ ...      │ ...   │ ...    │ ... │ ... │ ...  │
└──────┴──────────┴───────┴────────┴─────┴─────┴──────┘
```

#### 구성 요소
1. **KPI Cards (4개)** - Total P&L / Average P&L / Total Value / Positions
2. **Active Positions Table** - 9 columns with P&L highlighting (green/red)
3. **Actions** - Add / Close buttons per row

#### Streamlit 구현
- `st.columns(4)` + `st.metric()` with delta for KPIs
- `st.dataframe()` with conditional formatting for P&L colors
- Action buttons: Add column with button widgets (or use st.data_editor)

---

## 🚧 Streamlit 한계 및 대안

### 1. 고정 사이드바 (Fixed Sidebar)
- **V0**: 264px 고정 폭, 독립적 스크롤
- **Streamlit**: st.sidebar는 고정이지만 폭 조절 불가
- **대안**: 기본 사이드바 사용, 폭은 Streamlit 기본값 수용

### 2. Workflow Progress (5-step 파이프라인)
- **V0**: SVG 기반 연결선 + 원형 아이콘 + 진행 상태 색상
- **Streamlit**: 복잡한 SVG 렌더링 어려움
- **대안**:
  - `st.columns(5)` 사용
  - 각 컬럼에 상태 아이콘(✅/⏳/○) + 텍스트
  - 단순한 텍스트 기반 진행 표시

### 3. Master-Detail 선택 하이라이트
- **V0**: 선택된 아이템에 파란 테두리 하이라이트
- **Streamlit**: 컨테이너 스타일링 제한적
- **대안**:
  - `st.info("📌 Selected")` 추가로 선택 표시
  - 또는 배경색 이모지 변경 (🔵 Selected)

### 4. 인터랙티브 데이터 테이블
- **V0**: 정렬, 필터링, 인라인 편집 가능한 테이블
- **Streamlit**: st.dataframe()은 읽기 전용 (정렬만 가능)
- **대안**:
  - `st.dataframe()` 사용 (기본 정렬 기능 제공)
  - 필터링은 상단 위젯으로 처리
  - 편집 필요시 `st.data_editor()` 사용

### 5. Toggle 버튼 (Buy/Sell)
- **V0**: 세그먼트 컨트롤 스타일 (양쪽 버튼, 하나만 선택)
- **Streamlit**: 기본 토글 위젯 없음
- **대안**:
  - `st.radio()` with horizontal layout
  - 또는 `st.columns(2)` + 2개 버튼 (선택 상태 강조)

### 6. 다크 테마
- **V0**: 완전한 다크 테마 (블랙 배경)
- **Streamlit**: 테마 설정으로 다크 모드 가능하지만 완벽한 제어 어려움
- **대안**:
  - `.streamlit/config.toml`에서 다크 테마 설정
  - Custom CSS 최소화 (기본 컴포넌트만 사용 조건)

### 7. 차트/그래프
- **V0**: 커스텀 차트 (Status Distribution 가로 막대)
- **Streamlit**: `st.bar_chart()`, `st.plotly_chart()` 사용 가능
- **대안**:
  - 간단한 시각화: `st.progress()` 여러 개 사용
  - 복잡한 차트: Plotly 또는 간단한 텍스트 기반 표현

### 8. 배지/태그
- **V0**: 커스텀 배지 (BUY, MONITOR, High Priority)
- **Streamlit**: 기본 배지 컴포넌트 없음
- **대안**:
  - `st.success()`, `st.warning()`, `st.error()` 활용
  - 백틱(`)으로 강조: `BUY`, `92`
  - 이모지 조합: 🟢 BUY, 🟡 MONITOR, 🔴 REJECT

---

## 📋 구현 우선순위

### Phase 1 (핵심 레이아웃)
1. ✅ Dashboard - KPI + Action Items + Stats
2. ✅ AI Reports - Master-Detail 구조
3. Stock Pool - 데이터 테이블

### Phase 2 (거래 기능)
4. Approval Queue - 승인 대기 리스트
5. Trading - 주문 폼
6. Active Trades - 포지션 모니터링

### Phase 3 (최적화)
7. 성능 최적화 (캐싱)
8. 반응형 레이아웃 조정
9. 에러 처리 및 엣지 케이스

---

## 🎨 디자인 토큰 (Streamlit 기본 컴포넌트 활용)

### 색상 의미
- **Green** (#65A150): Success, Positive P&L, Buy
- **Yellow** (#C5A940): Warning, Monitor, Pending
- **Red** (#C75545): Danger, Negative P&L, Reject
- **Blue** (#4D8FC7): Info, Selected, Neutral
- **Purple** (#5560C7): Primary actions

### 컴포넌트 매핑
- **KPI Cards** → `st.metric(label, value, delta)`
- **Status Messages** → `st.success()`, `st.warning()`, `st.error()`, `st.info()`
- **Data Tables** → `st.dataframe()` or `st.data_editor()`
- **Buttons** → `st.button()` with `type="primary"` for main actions
- **Layout** → `st.columns()`, `st.container()`, `st.expander()` (금지됨)
- **Tabs** → `st.tabs()` for multi-section content

---

## ✅ 완성 목표 체크리스트

- [ ] Dashboard에서 전체 시스템 상태를 한눈에 파악 가능
- [ ] AI Reports에서 리스트 선택 → 상세 정보 흐름 자연스러움
- [ ] Stock Pool에서 500개 종목을 효율적으로 탐색 가능
- [ ] Trading에서 주문 실행 및 계좌 정보를 명확하게 표시
- [ ] Approval Queue에서 빠른 의사결정 가능
- [ ] Active Trades에서 모든 포지션의 P&L을 실시간 확인 가능
- [ ] 모든 페이지에서 "V0 디자인 기반"이라는 느낌 유지
- [ ] Streamlit 기본 컴포넌트만 사용하면서도 정보 밀도 높음
